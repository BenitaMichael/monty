#include "monty.h"

/**
* monty_stack - function that sets the format of the data to a stack (LIFO)
* This is the default behavior of the program
* @head: head of the stack
* @line_count: line count
* Return: nothing (void)
*/
void monty_stack(stack_t **head, unsigned int line_count)
{
	(void)head;
	(void)line_count;
	func.fl = 0;
}
